using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMototSounds : MonoBehaviour
{
    [SerializeField] private GameObject lowSpeedFadeSound;
    [SerializeField] private GameObject medSpeedFadeSound;
    [SerializeField] private GameObject highSpeedFadeSound;
    
    [SerializeField] public GameObject player;

    //private playerMovmentScript;
    //private PlayerMovement
    // Start is called before the first frame update
    void Start()
    {
        //player_movement_Script = player.GetComponent<PlayerMovement>();
    }
    
    // Update is called once per frame
    void Update()
    {
       //playerMovementScirpt.Thrust = 10;
       // if(player.GetComponent<PlayerMovement>()Thrust)
        {
            
        }
    }
}
